<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace="pork">
     
     <select id="userLogin" parameterType="com.mmhouse.pork.vo.UserVO" resultType="com.mmhouse.pork.vo.UserVO">
        <![CDATA[
            SELECT
                   email,
                   cellPhone,
                   name,
                   birth,
                   address,
                   addressDetail,
                   useNavi,
                   autoRecommand,
                   pushRecv
                   
              FROM
                users
             WHERE email = #{email}
               AND pwd = #{pwd}
        ]]>
    </select>
    
    <select id="checkUser" parameterType="hashmap" resultType="hashmap">
        <![CDATA[
            SELECT
                   COUNT(*) count
                   
              FROM
                   users
             WHERE email = #{email}
        ]]>
    </select>
    
    <insert id="joinUser" parameterType="hashmap">
        <![CDATA[
            INSERT users
 			       (           
                   email,
                   pwd,
                   name,
                   cellPhone,
                   birth,
                   useNavi,
                   autoRecommand,
                   pushRecv,
                   inst_dt,
                   updt_dt
                   )
              VALUES
                   (
                   #{email},
                   #{pwd},
                   #{name},
                   #{cellPhone},
                   #{birth},
                   #{useNavi},
                   #{autoRecommand},
                   #{pushRecv},
                   now(),
                   now()
                   )
        ]]>
    </insert>
    
    <insert id="writeContents" parameterType="hashmap">
        <![CDATA[
            INSERT contents
 			       (           
                   contType,
                   email,
                   contTitle,
                   contMemo,
                   secretYn,
                   address,
                   latitude,
                   longitude,
                   inst_dt,
                   updt_dt
                   )
              VALUES
                   (
                   #{contType},
                   #{email},
                   #{contTitle},
                   #{contMemo},
                   #{secretYn},
                   #{address},
                   #{latitude},
                   #{longitude},
                   now(),
                   now()
                   )
        ]]>
    </insert>
    
    
    <insert id="writeContentsMedia" parameterType="hashmap">
        <![CDATA[
            INSERT writeContentMedia
 			       (   
 			       contId,        
                   contType,
                   mediaId,
                   mediaName,
                   mediaSize,
                   address,
                   latitude,
                   longitude
                   )
              VALUES
                   (
                   #{contId},
                   #{contType},
                   #{mediaId},
                   #{mediaName},
                   #{mediaSize},
                   #{address},
                   #{latitude},
                   #{longitude}
                   )
        ]]>
    </insert>
    
    <select id="getContId" parameterType="hashmap" resultType="hashmap">
        <![CDATA[
            SELECT
                   MAX(contId) as contId
              FROM contents
             WHERE email = #{email}
        ]]>
    </select>
    
</mapper>